These code files were used in V2G controller simulations. Each folder is self-contained.

To run each MATLAB app, open the .mlapp file in the respective folder and ensure this folder is in your MATLAB directory.

The genetic algorithm code is provided as reference for how the chromosome was used to set up the FIS and fitness function.

I have tried to catch all bugs in the code but there is no guarantee that this is the case.
